import { useState } from 'react'; import { Send } from 'lucide-react'; import { api } from '../api/client'; import MessageBubble from '../components/MessageBubble'
export default function Chat(){ const [messages,setMessages]=useState([{role:'assistant',content:'你好，我是 RAG 求职知识库助手。请上传资料后向我提问。',sources:[]}]); const [q,setQ]=useState(''); const [busy,setBusy]=useState(false); const send=async e=>{e.preventDefault();if(!q.trim()||busy)return;const question=q;setQ('');setMessages(m=>[...m,{role:'user',content:question},{role:'assistant',content:'思考中…',sources:[]}]);setBusy(true);try{const d=await api.chat({question});setMessages(m=>[...m.slice(0,-1),{role:'assistant',content:d.answer||'服务返回了空回答，请检查模型配置或稍后重试。',sources:d.sources||[],latency_ms:d.latency_ms}])}catch(err){setMessages(m=>[...m.slice(0,-1),{role:'assistant',content:err.message,sources:[]}])}finally{setBusy(false)}};return <section className="page chat-page"><header><div><h1>知识库对话</h1><p>基于你的文档回答，所有结论附带来源</p></div><span className="status">● 在线</span></header><div className="messages">{messages.map((m,i)=><MessageBubble key={i} message={m}/>)}</div><form className="composer" onSubmit={send}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="询问 RAG、面试题或你的求职材料…"/><button title="发送"><Send size={18}/></button></form></section> }
