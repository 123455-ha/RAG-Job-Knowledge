export default function MessageBubble({message}) { return <div className={`message ${message.role}`}><div className="avatar">{message.role==='user'?'你':'AI'}</div><div><div className="bubble">{message.content}</div>{message.sources?.length>0&&<div className="sources">{message.sources.map(s=><div className="source" key={s.chunk_id}><b>{s.source_type==='web'?'🌐 ':''}{s.file_name}</b>{s.page&&` · 第 ${s.page} 页`}<span>{s.score}</span><p>{s.snippet}</p>{s.url&&<a href={s.url} target="_blank" rel="noreferrer">查看网页来源</a>}</div>)}</div>} {message.latency_ms!=null&&<small>{message.latency_ms} ms · {message.sources.length} 个来源</small>}</div></div> }
